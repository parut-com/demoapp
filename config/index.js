export const oauthClientId = process.env.OAUTH_CLIENT_ID;
export const oauthPrividerUrl = process.env.OAUTH_PROVIDER_URL;

const config = {
  oauthClientId,
  oauthPrividerUrl
};

export default config;
